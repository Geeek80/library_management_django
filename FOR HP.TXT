# in settings.py
# comment your DATABASE list and add this list below

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'library_mgmt', # make database in mysql and provide name here
        'USER': 'root',     # database user
        'PASSWORD': '',
        'HOST': '127.0.0.1',
        'PORT': '3306',     # see port number of sql in xamp panel
    }
}

# run command   
# python manage.py makemigrations
# python manage.py migrate

# after this tables should be automatically created in your database

# add a record in user table from phpmyadmin

# then test the login functionality using those credentials

# check console if 'user not found'  is printed that means login failed